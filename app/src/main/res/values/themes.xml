<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. The Compose UI supplies the real Material3 theme;
         this XML theme only needs to be a valid Material3 parent without an action bar. -->
    <style name="Theme.PunchReminder" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
